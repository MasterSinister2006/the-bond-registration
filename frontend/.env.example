# In local development, leave this empty to use the Vite /api proxy if configured.
# In Vercel production, set VITE_API_URL to your Render backend URL, e.g.
# https://the-bond-backend.onrender.com/api
VITE_API_URL=
